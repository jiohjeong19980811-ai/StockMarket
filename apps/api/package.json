{
  "name": "@stockmarket/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "dev": "node --watch --import tsx src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@stockmarket/data": "0.1.0",
    "@stockmarket/db": "0.1.0",
    "@stockmarket/paper-trading": "0.1.0",
    "@stockmarket/scoring": "0.1.0",
    "fastify": "^5.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0"
  }
}
